const resourcePrefix = 'dynamodb-professional';

export const usersTable = `${resourcePrefix}-users`;
export const teamIdGSI = 'TEAM_ID_GSI';

export const documentsTable = `${resourcePrefix}-documents`;
